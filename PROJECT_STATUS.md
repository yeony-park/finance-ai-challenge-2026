# DAKER 작업 인수인계

기준 시점: 2026-08-30 KST

## 현재 범위

- 사용자 UI와 카탈로그는 미술품 조각투자 전용입니다.
- 운영 데이터는 `data/synthetic/art-investment.json` 한 개만 사용합니다.
- 현재 합성 상품 9건, 합성 과거 이력 318건, 가상 플랫폼 3곳입니다.
- 구좌당 공모가는 100,000원이며 총 공모금액·취득가·비용·구좌 수 관계를 일관되게 생성합니다.
- 합성 과거 이력은 원본 행을 변형하지 않고 구간별 금액·기간·상태 분포를 독립 생성합니다.
- OpenDART 코드는 서버 전용으로 유지하지만 합성 상품과 실제 접수번호를 연결하지 않습니다.
- 원본 자료 22개는 서비스 저장소 밖의 비공개 보관 경로에 SHA-256 manifest와 함께 보존했습니다.

## 배포 상태

- 현재 트리·Next 런타임·Python/static 경로는 합성 데이터 전용으로 전환했습니다.
- Docker는 allowlist `COPY`만 사용합니다.
- 전체 테스트와 현재 source/production artifact에 대한 독립 보안 검토는 PASS입니다.
- Git 이력 정리와 기존 registry/CI/CDN cache 폐기는 아직 별도 승인 작업으로 남아 있습니다.
- 이 환경에는 Docker CLI가 없어 최종 이미지 export 검사를 실행하지 못했습니다. Docker 이미지 검사와 이력·cache 정리가 끝날 때까지 실제 배포 판정은 BLOCK입니다.

## 검증

`README.md`와 `AGENTS.md`의 합성 데이터 검증 명령을 실행합니다. 네트워크 검사는 결정적 회귀 테스트와 분리합니다.
