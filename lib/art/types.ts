export type Verdict = "worth_considering" | "conditional" | "caution" | "danger";
export type VerdictLabel = "해볼 만함" | "조건부 해볼 만함" | "주의" | "위험";
export type OfferingStatus = "upcoming" | "open" | "operating" | "exit_in_progress" | "liquidated" | "unverified";
export type DisclosedCost = { category: string; label: string; amount: number };
export type CareerRecord = { type: "solo" | "group" | "collection" | "award" | "gallery"; title: string; year: number; sourceId: string };
export type Artwork = { id: string; artistId: string; title: string; productionYear: number | null; medium: string | null; width: number | null; height: number | null; depth: number | null; edition: string | null; series: string | null; provenance: string | null; condition: string | null; imageUrl: string | null; sourceIds: string[] };
export type Artist = { id: string; nameKo: string; nameEn: string | null; birthYear: number | null; deathYear: number | null; nationality: string | null; biography: string | null; officialCareer: CareerRecord[]; imageUrl: string | null; sourceIds: string[] };
export type Offering = { id: string; slug: string; artworkId: string; artistId: string; platformId: string; issuerId: string; title: string; status: OfferingStatus; subscriptionStart: string | null; subscriptionEnd: string | null; unitPrice: number | null; minimumInvestment: number | null; numberOfUnits: number | null; totalOfferingAmount: number | null; acquisitionPrice: number | null; appraisalValue: number | null; targetHoldingMonths: number | null; actualHoldingMonths: number | null; distributionTerms: string | null; exitMethod: string | null; midTermTransferAvailable: boolean | null; disclosedCosts: DisclosedCost[]; actualDistributionAmount: number | null; actualExitAmount: number | null; asOfDate: string; updatedAt: string | null; sourceIds: string[]; isDemo: boolean };
export type AuctionRecord = { id: string; artistId: string; artworkTitle: string; auctionDate: string; auctionHouse: string; country: string | null; lotNumber: string | null; productionYear: number | null; medium: string | null; width: number | null; height: number | null; depth: number | null; edition: string | null; series: string | null; estimateLow: number | null; estimateHigh: number | null; hammerPrice: number | null; realizedPrice: number | null; currency: string; normalizedPriceKRW: number | null; result: "sold" | "unsold" | "withdrawn" | "unknown"; reportedPrice?: number | null; priceBasis?: string | null; verificationStatus?: string | null; sourceIds: string[] };
export type ComparableRecord = { id: string; offeringId: string; auctionRecordId: string; sameSeries: boolean; sameMedium: boolean; similarSize: boolean; similarYear: boolean; similarityScore: number; comparisonReason: string };
export type Platform = { id: string; name: string; operatorName: string | null; website: string | null; issuerIds: string[]; sourceIds: string[] };
export type Issuer = { id: string; legalName: string; registrationNumber: string | null; platformIds: string[]; sourceIds: string[] };
export type TrackStatus = "offering" | "operating" | "exit_in_progress" | "sold" | "liquidated" | "delayed" | "unsold" | "loss_confirmed";
export type TrackRecord = { id: string; platformId: string; issuerId: string | null; offeringId: string | null; productName: string; artworkTitle: string; artistName: string; offeringAmount: number | null; targetHoldingMonths: number | null; actualHoldingMonths: number | null; totalDistribution: number | null; exitAmount: number | null; finalReturn: number | null; status: TrackStatus; delayDays: number | null; soldAt: string | null; liquidatedAt: string | null; sourceIds: string[]; rawStatus?: string | null; sourceLabel?: string | null; sourceUrl?: string | null; isSelfReported?: boolean; currencyNote?: string | null; evidenceNote?: string | null; identityStatus?: string | null; legalIssuerStatus?: string | null; reportedAmount?: number | null; reportedReturn?: number | null; legacySourceRef?: string | null; sourcePayload?: unknown; dueDiligencePayload?: unknown };
export type Evidence = { id: string; entityType: string; entityId: string; fieldPath: string; claim: string; value: unknown; sourceTitle: string; sourcePublisher: string; sourceUrl: string | null; sourceType: string; asOfDate: string | null; collectedAt: string | null; formula: string | null; notes: string | null };
export type AnalysisSection = { conclusion: string; quantitativeFindings: string[]; qualitativeFindings: string[]; evidenceIds: string[] };
export type KeyReason = { title: string; finding: string; implication: string; evidenceIds: string[] };
export type AnalysisResult = { offeringId: string; methodologyVersion: "art-mvp-v1.0"; verdict: Verdict; verdictLabel: VerdictLabel; headline: string; summary: string; keyReasons: KeyReason[]; priceInsight: AnalysisSection; artistInsight: AnalysisSection; exitInsight: AnalysisSection; platformInsight: AnalysisSection; missingInformationRisks: string[]; conflicts: string[]; evidenceIds: string[]; updatedAt: string };
export type AnnualAuctionMetric = { year: number; offered: number; sold: number; medianPrice: number | null; unsold: number };
export type ChangeLog = { id: string; entityType: string; entityId: string; fieldPath: string; previousValue: unknown; newValue: unknown; changedAt: string; sourceIds: string[] };
export type ArtDataset = { offerings: Offering[]; artworks: Artwork[]; artists: Artist[]; auctions: AuctionRecord[]; comparables: ComparableRecord[]; platforms: Platform[]; issuers: Issuer[]; trackRecords: TrackRecord[]; evidence: Evidence[]; analyses: AnalysisResult[]; annualMetrics: Record<string, AnnualAuctionMetric[]>; changeLogs: ChangeLog[] };
export type ProductView = { offering: Offering; artwork: Artwork; artist: Artist; platform: Platform; issuer: Issuer; analysis: AnalysisResult; auctions: AuctionRecord[]; comparables: Array<ComparableRecord & { auction: AuctionRecord }>; trackRecords: TrackRecord[]; evidence: Evidence[]; annualMetrics: AnnualAuctionMetric[] };
export type ParsedSearchQuery = { keyword?: string; offeringStatus?: OfferingStatus[]; verdict?: Verdict[]; artistNames?: string[]; platformNames?: string[]; premiumMin?: number; premiumMax?: number; auctionVolumeMin?: number; sellThroughRateMin?: number; delayedExitOnly?: boolean; sort?: string };
