# 조각 히어로 — 첫 화면 구성 교체 (2026-08-22)

시안 v4(아티팩트 855f9a1c, 오너 승인) 실장. 정체성(종이톤·노랑·잉크·판정 3색 체계) 무변경,
홈 첫 화면 레이아웃만 교체.

## 결정과 근거

- **스플릿 풀하이트 히어로**: 좌 = 타이틀·리드·검색 스캐폴드(칩 6→3 압축), 우 = 어긋난
  마름모 조각 4개. 마름모 = 조각투자의 "조각" 은유, 어긋난 배치 = "아직 다 모이지 않은
  조각" 서사. 오너 지시(2026-08-22): 정사각 격자 금지.
- **상태 문법**: 실선+노랑 엣지+선명 사진 = 리포트 공개 중 / 점선+탈색 사진 = 준비 중.
  층별 지원 선언의 정직성을 형태로 표현. 상태는 `CATEGORY_REGISTRY.preview`에서만 파생
  (새 상태 축 신설 없음).
- **아이콘 전면 배제** (오너: "무게감이 떨어진다"): 라인아트 모티프 4종 포함 전 표면 제거
  — CategoryGrid·CategoryLanding에서 사용처 제거 후 `site/icons.tsx` 삭제. 스캔형 개편의
  "모티프 4종만 유지" 결정을 대체. 재료는 타이포·기하·사진 3종.
- **홈 헤더 내비 제거** (홈 한정): SiteNav가 `pathname === "/"`에서 null. 카테고리 진입 =
  조각, 검증 리포트 = 04 그리드 링크·확인 질문 다리, 검증 방법 = 푸터. 착지·리포트
  화면은 상호 이동 필요로 내비 유지.
- **사진 레이어**: 각 착지가 쓰는 `category-*.jpg` 재사용(자체 자산 — `05` §3 상태 불변).
  마름모 클리핑 = `rotate(45deg)+overflow:hidden`, 사진·내용 역회전으로 수평 유지.
  `hero-dossier.jpg`(서류철)는 홈에서 퇴역 — 02 밴드 이관 여부는 오너 결정 대기.
- **신규 문안 4종**(eyebrow·공적 출처 라인·조각 상태 2값)은 `content/home.ts` 단일 진실 +
  출력 필터 감사(ALL_COPY) 등재. 조각 상태 어휘가 판정 어휘를 침범하지 않는 테스트 추가.

## 트레이드오프

- 마름모 클러스터는 데스크톱(>68rem) 전용 — 이하에서는 회전 해제 후 2×2 사진 카드로
  강등(터치 타깃·스캔성 우선). 첫인상 장치를 포기하는 대신 기존 카드 문법 재사용.
- 예시 질문 칩 6→3 압축: 히어로 밀도 확보 대신 나머지 질문 노출은 후속 질문 칩에 위임.
- 히어로 실물 사진(서류철) 제거: "실물 실재감" 전달을 카테고리별 사진 4장이 대신.
- deco 조각 2점(노랑 미니·점선 고스트)은 장식 — `aria-hidden`+`pointer-events:none`,
  모바일에서 숨김.

## eval 영향

- 판정·마스킹·공개 경로 로직 무변경. vitest 57파일 924통과(+2 skip) — 신규: 조각 히어로
  문안 정합 2건(칩 전단사·상태 어휘 비침범), 건축HUB 어댑터 27건(별건 커밋).
- `next build` 21페이지 통과. 실서버 Playwright 육안: 데스크톱 1440(클러스터·호버·노랑
  엣지), 태블릿 1000·모바일 390(카드 강등), 홈 그리드·한우 착지(아이콘 제거 확인),
  스크롤 섹션 리빌.
- 모바일 강등 초판에서 `.shard{position:static}`이 사진 absolute 기준을 클러스터로 승격
  시켜 마지막 사진이 전체를 덮는 결함 — `position:relative`+오프셋 리셋으로 수정 후 재검증.

## 알려진 한계

- 내비 제거 범위는 홈 한정(제안 채택 상태) — 전 페이지 확대는 대체 동선 설계 선행 필요.
- `hero-dossier.jpg`는 현재 미사용 자산으로 남음(퇴역만 확정, 행선지 미정).
- 미술품 탈색 사진이 한돈 대비 시각적으로 무거움(원본 명도 차이) — 사진 교체 없이는
  베일 수치 공통이라 남는 편차.
- 조각 상태 2값("리포트 공개 중"/"준비 중")은 카테고리 노트 문안의 축약 — 레지스트리
  노트와 의미 정합은 유지되나 문안 단일 진실이 두 층위(노트·상태)로 늘었다.
