# S3.5 및 후속 작업 로그 — 판정 유효성 회복·커버리지 확장 (2026-08-14~15)

> 기록 규약은 `README.md`. S3.5 본편(D1~D3, 8/14)은 커밋 b362a70·9eaef1c·c481138과
> `docs/planning/페이즈-계획.md`의 S3.5 절이 1차 기록이다. 이 파일은 worklog 도입일(8/15)
> 당일 작업분부터 상세 기록한다.

## 성별 판정 재분류·판정 어휘 통일 (2026-08-15, 커밋 f672034·d173637)

**결정과 근거** — 가축 7호(40두)·8호(66두) 리포트에서 전 개체가 성별 항목만
불일치로 판정되는 현상을 실측(신고서 "수" vs 원장 "거세", 106건 전량). 한우
수컷의 거세는 비육 표준 관행이므로 이는 리스크 신호가 아닌 예상된 상태 전이다.
`assessSex`에서 수→거세를 match + 전이 각주로 재분류하고, 불가능 전이(암↔수
등)만 불일치로 유지했다. 동시에 mismatch의 화면 라벨 "원장 미확인"을 "원장
불일치"로 정정 — "미확인" 어휘는 unverifiable 전용으로 분리했다(어휘 혼용이
판정 3값 체계의 의미를 흐림). 재분류 후 라이브 재대조 결과 7호 200/200·8호
330/330 일치, 9호는 보관장소 1건(정탐)만 잔존.

**트레이드오프** — 도메인 상식을 판정 규칙에 하드코딩하는 선례를 만들었다.
대안은 "모든 차이를 기계적으로 불일치 처리 + 화면에서 각주"였으나, 노이즈
106건이 정탐 1건(9호 보관장소)을 매몰시켜 리포트 실효성이 무너지는 실측이
있어 기각. 지불한 비용: 축종별 전이 규칙 테이블을 유지보수해야 하며, 규칙이
틀리면 오탐이 아니라 미탐(놓침)이 된다.

**검증 영향** — judge-engine 테스트에 전이 케이스 고정. "원장 미확인" 문자열
전수 스윕 0건이 게이트. 7·8호 라이브 재대조 결과가 새 회귀 기준 스냅샷.

**알려진 한계** — 성별은 전면 판정에서 내려가 개체 동일성 보조 필드 + 경락가
지수 선택 입력으로만 쓰인다. 거세 시점 조작 같은 신호는 이 재분류로 잡지
못한다(원장 자체를 신뢰하는 구조적 한계).

## 정정 전→후 대비 발췌 (2026-08-15, 커밋 f576529·6db1695)

**결정과 근거** — 정정신고서 본문의 발행사 자필 "(주N) 정정 전/후" 블록을
파싱해 리플레이 항목별 전→후 2단 발췌를 만들었다. 근거 원문을 심사자가 화면에서
직접 대조할 수 있어야 "정정 재검증" 서사가 성립한다. 변경 지점 포커스(600자
상한)·접기 UI 적용. 7·8호 리플레이를 실제 정정 발췌 포함으로 재생성했다.

**트레이드오프** — (주N) 블록 형식은 발행사 자필이라 표기 편차가 있다 —
파싱은 뱅카우 계열 신고서에서 실측된 형식 기준이며, 타 발행사 신고서에서는
발췌가 비거나 부정확할 수 있다(그 경우 발췌 없이 판정만 표시하는 정직 폴백).

**검증 영향** — 발췌 경로로 발행사명이 노출되는 것을 익명화 게이트 테스트가
실제로 잡아냈고, `maskFreeText`에 프로파일 issuer 기반 브랜드 토큰 마스킹을
추가했다. 재생성 산출물 익명화 검증 누출 0건.

**알려진 한계** — 발췌는 텍스트 대비만 지원 — 표 구조 변경은 원문 좌표
링크로만 확인 가능.

## 커버리지 확장 — 가축 1~6호 사후 검증 등록 (2026-08-15, 커밋 cc44cce·e3bb463)

**결정과 근거** — 2024~25년 종료 공모 6건(가축 1~6호)을 사후 검증으로 등록해
공개 리포트 10건 체제(2026 코호트 상한 3/8 + 사후 7건)로 확장했다. 커버리지가
"전수 대비 몇 건"으로 화면에 찍히는 구조라, 등록 수 자체가 확장성의 증명이다.
구형 신고서에서 파싱 결함 2건을 실측·수정: 이력번호 선행 0 소실 복원, 보관장소
셀 줄바꿈 정규화(교차검증 구분자 무시). 경락가 캐시를 2023-11~2025-12로 확장해
사후 검증 전 개체의 가격 위치를 제시했다. 라이브 재대조 결과 신규 실측 신호
4건 — 2호 충만9호 원장 미조회(실재 확인 불가), 4호 에덴 10·20호와 5호 에덴
125호 보관장소 불일치.

**트레이드오프** — 페이즈 계획의 축소 스위치 방향(커버리지를 줄여 부하를
낮춤)과 반대로 확장을 선택했다. 근거: 파서·어댑터가 이미 일반화돼 있어 등록
1건당 한계 비용이 낮았고, 사후 검증 리포트가 트랙레코드 서사(정정 계보 4건
연쇄 등)를 실데이터로 채운다. 지불한 비용: 공개 산출물 재생성·익명화 스캔
대상이 10건으로 늘어 재검증 시간이 증가.

**검증 영향** — 3호는 첨부정정 원문 미제공 → 직전 정정본 재대조 폴백을 구현
(원문 부재를 실패가 아닌 정직 표기로). 전 건 익명화 스캔 누출 0건이 게이트.

**알려진 한계** — 2호 충만9호는 원장 미조회라 실재성 판정이 "대조 불가"로
남는다. 8-2 트랜치 66두 병합은 백로그.

## 정정 기록 문서형 개편·리포트 가독성 3종 (2026-08-15, 커밋 e00f63c·de8d3b1)

**결정과 근거** — 정정 이력 섹션의 플레이어 메타포(재생 컨트롤 중심)를 제거하고
문서형(기록 나열형)으로 개편 — 심사자·투자자가 기대하는 건 "재생"이 아니라
"기록 열람"이라는 판단. 가독성 3종 추가: 방법론 4단계를 번호 리스트로 분리,
전→후 발췌에 공통 접두·접미부 기반 변경 구간 하이라이트(전=취소선 미스 톤,
후=매치 톤), 헤더 아래 sticky 챕터 네비게이션(요약·정정 이력·이행 이력·실재
확인·가격 위치) + scroll-margin 보정.

**트레이드오프** — 하이라이트는 공통부가 없으면 강조를 생략한다(잘못된 diff
표시는 오해를 만들므로 보수적으로). 챕터 네비는 리포트 섹션 구성이 바뀌면
동기화가 필요한 결합점이 하나 늘었다.

**검증 영향** — `actual-replay-view` 테스트에 하이라이트 케이스 고정.

**알려진 한계** — `report.module.css` 분할(986줄) 백로그 지속.

## 문서 정비 — README·페이즈 계획·worklog 도입 (2026-08-15, 이 세션)

**결정과 근거** — README "현재 상태"가 8/10에 멈춰 있어(폐기된 "카사 10개
건물" 데모 축 잔존) 8/15 기준으로 전면 재작성. git 공개 문서 톤 기준으로
이모지·수사적 문장을 제거하고 표 기반 기술 서술로 정돈. `AI_GATEWAY_API_KEY`
권장 표기가 실구현(OpenAI 직결 폴백)과 어긋나 사실 교정. 페이즈-계획에 S3.5
후속 게이트 통과·8/15 계획 외 실행분·"9호 실정정 미반영" 리스크를 반영.
AI 작업 수정 내역의 상세 기록처로 curation-gate의 worklog 규약을 이식해
`docs/worklog/` 신설.

**트레이드오프** — README의 8/10 이전 상세 이력(Phase 0~1 게이트 항목별
기록)을 한 줄로 압축 — 상세는 페이즈-계획과 리서치 문서로 위임. worklog는
S0~S3.5 소급 재작성을 하지 않기로 규약에 명시(커밋 메시지가 1차 기록).

**검증 영향** — 해당 없음(문서).

**알려진 한계** — worklog 병행 기록은 규약일 뿐 강제 장치(훅·게이트)가 없다.
커밋 전 worklog 갱신을 잊으면 공백이 생긴다.

## 가축 9호 실정정 재대조·리플레이 전환 (2026-08-16)

**결정과 근거** — 8/14 감시가 포착한 기재정정(rcpNo 20260814003572)을 실정정
경로로 반영했다. 원문 수집(베이스 20260806000159 포함 — integration 워크트리는
`data/raw/`가 로컬 전용이라 비어 있었음) → fake 선검증(scratch-fake 격리) →
라이브 재대조 → narrative 재생성 → 합성 데모 diff 제거 순서로, 7·8호에서
검증된 경로를 그대로 탔다. 실측: 교차검증 185건(규칙 222·LLM 185·상충 0),
판정 유지 185·변동 0, 개체 24호 보관장소 불일치 1건 잔존(8/10 첫 실호출부터
추적된 정탐), 대조 불가 1건(취득시기 30일 창 기록 부재). 정정 내용 실측에서
**청약 일정 변경 발견**: 8/27~9/10 → **9/8~9/22**(납입 9/23, 금감원 제출 요구에
따른 기재 보완, 모집총액 363,320,000원 불변) → `OFFERS` 레지스트리 갱신,
`OFFER_REGISTRY`에 정정 rcpNo 등록. 부수 결함 수정: `actual-replay-cli`의
"청약이 종료된 공모" 고지가 하드코딩이라 청약 전 공모인 9호에 사실과 다른
문안이 붙을 뻔함 → `replay-notes.ts` 신설로 phase(open/closed) 분기, closed
문안은 7·8호 기존 산출물과 동일하게 고정.

**트레이드오프** — (a) 합성 데모 diff를 남겨두는 대신 삭제: 로더가 최신만
읽어 노출은 안 되지만, 실정정과 합성이 공존하면 정렬에만 의존하는 이중
상태가 된다 — git 히스토리에 남으므로 삭제를 택했다. (b) 최신 리포트가
정정본(8/14 제출) 기준으로 바뀌며 리포트 화면의 문서 버전 표기가 교체됐다 —
정정 전 리포트는 버전링(18건 보관)으로 접근 가능. (c) open-phase 고지는 청약
상태를 아예 단정하지 않는 문안으로 작성 — 청약 진행·종료 후에도 낡지 않게
하기 위해 정보량을 일부 포기했다.

**eval 영향** — 익명화 게이트를 리플레이 diff까지 확장(`public-report.test.ts`
— 정정 발췌의 9자리+ 숫자열·주소·행정구역 스캔, 종전에는 리포트 파일만
자동 검사·diff는 수동이었음). phase 분기 문안 회귀 테스트 4건 신설
(`amend-replay-notes.test.ts` — closed 문안을 기존 산출물과 문자열 동일로
고정). 데이터 결합 테스트 2편을 실정정 현실로 갱신: 합성 뷰 검증은 리포
데이터 의존을 끊고 인라인 픽스처로 분리, 리포트 ③층위 제출일 기대값
8/6→8/14. 금지어 목록에서 "불일치" 제거 — S3.5에서 mismatch 라벨이 "원장
불일치"로 공인된 뒤 남은 낡은 금지어였다. 스위트 545 통과·2 스킵(로컬
데이터 확보로 종전 스킵 26건이 실행 전환), tsc·lint 클린, build 완주.

**알려진 한계** — 정정으로 청약이 9/8~9/22가 되며 심사 기간(9/7~9/11) 내
"청약 종료 이벤트"가 사라짐 — Phase 5의 9/10 재배포 예외는 소멸 처리했고,
심사 기간 중 추가 정정 가능성은 잔존(감시 주 2회 유지). narrative는
LLM 생성물이라 실행마다 문면이 달라질 수 있다(필터 통과는 게이트가 보장,
easy/pro 각 4절 재생성 확인). 리플레이 diff 익명화 스캔은 livestock-9
디렉토리 한정 — 7·8호 diff는 커밋 전 수동 검증분이며 전 공모 확장은 후속.

## 잔여 (다음 항목 후보)

- ~~가축 9호 기재정정(8/14 접수, rcpNo 20260814003572) 실정정 재검증~~
  → ✅ 2026-08-16 완료 (위 항목)
- S4: 외부 사용성 테스트, 골드셋 222건 검수·F1, 기획서·기능명세서 전사
