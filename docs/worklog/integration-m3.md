# 통합 M3 워크로그 — 입문자 여정·개인화·카테고리 디자인

> 기록 규약은 `README.md`. M3 범위: 입문자 여정 설계 → 선언형 개인화(온보딩·프로파일) →
> 카테고리 디자인 체계. 오너 결정(2026-08-16): 개인화 저장은 로컬(계정 없음),
> 00-overview [팀 결정 대기] 항목은 기본값 그대로 확정.

## M3 개시 — 여정 설계·프로파일 코어·디자인 시안 (2026-08-16)

**결정과 근거** — 오너가 "입문자를 위한 설계 방식과 기술 스택 고민이 생략된 것 아닌가"를
제기했다. 점검 결과 입문자 설계의 재료(IDI 발견·확인 질문 8가지·눈높이 2단·표현 경계)는
이미 있으나 이를 잇는 끝-끝 여정과 단계별 성공 기준이 문서화된 적이 없어,
`docs/planning/입문자-여정.md`(여정 5단계·온보딩 문답 3개·프로파일 명세·스택 평가)를
신설했다. 개인화는 **선언형**으로 정의 — 사용자가 밝힌 것만 반영하고(level·concern·
interests 3필드), UI는 프로파일의 순수 함수로 정렬·강조만 한다(숨김 금지). 근거는 04
라우팅 중립성(AI 주도 선별·우선 노출 금지)과 PRD의 "AI 주도 상품 라우팅" 사각 경고.
코드는 `src/components/site/profile.ts` — watchlist의 useSyncExternalStore 선례를
미러(파싱은 저장값 불신뢰, 차단 시 메모리 폴백). 카테고리 디자인은 한우 착지 재설계
시안(아티팩트, 저장소 외부)으로 선행 검증: 서류철 탭 내비, 층 선언 스탬프 카드, 시그니처
데이터 모듈(거세 경락가 33개월 실데이터 SVG), 사건·정정 타임라인, 확인 질문→리포트 챕터
다리, 카테고리 액센트 4색. 액센트는 dataviz 검증기로 실측 — 초안 2회 FAIL(3색이 회색으로
읽힘, 미술품↔부동산 ΔE 3.9로 색각이상 구분 불가) 후 확정: 한우 #8f3d1f · 돼지 #c2497c ·
미술품 #00879f · 부동산 #4653c9 (명도 대역·채도 하한·정상 분리도·표면 대비 전 항목 PASS,
색각이상 인접쌍 ΔE 7.7 = 텍스트 라벨 동반 시 허용 대역 — 카테고리는 항상 문자 병기이므로
구조적으로 충족).

**트레이드오프** — (a) 온보딩 UI·문안 구현을 이번에 하지 않고 여정 문서·시안에 대한 오너
반응 뒤로 미뤘다: 문안은 필터 감사·오너 리뷰 대상이라 선구현 시 재작업 위험이 컸다. 대신
방향 무관한 코어(프로파일 저장·정렬 함수)만 선구현. (b) 프로파일 필드를 3개로 제한(열람
이력 자동 수집 없음): 개인화 품질의 상한을 낮추는 대신 선언형 원칙·개인정보 최소화를
지켰다. (c) 액센트 채도를 검증기 통과선까지 올렸다: "종이톤에 녹는 저채도" 심미가 일부
양보됐다 — 회색으로 읽히는 색은 기능하지 않는다는 실측이 우선. (d) 시안을 저장소 밖
아티팩트로 뒀다: 팀 히스토리가 깨끗한 대신 시안 자체는 버전 관리 밖 — 확정 값은
tokens.css·design-system.md 반영 시점에 저장소로 들어온다.

**eval 영향** — 여정 5단계의 성공 기준 5문장이 S4 사용성 테스트 태스크의 뼈대로 신설됨.
프로파일 테스트 16건 신규(`profile.test.ts`) — 저장값 불신뢰 파싱, 정렬 순수성, **원소
보존(숨김 금지) 불변식**을 테스트로 고정. 액센트 팔레트는 검증기 실행 기록(2026-08-16,
surface #ffffff, adjacent)으로 회귀 기준 확보. 검증: 신규 스위트 16 통과, tsc·lint 클린
(빌드 무관 — 미참조 리프 모듈).

**알려진 한계** — 프로파일 store의 window 의존부(useSyncExternalStore·storage 이벤트)는
유닛 테스트 밖(watchlist과 동일한 공백). 온보딩 문안·개인화 상태 표시줄·레지스트리 파생
내비는 미구현 — 시안 확정 후 착수. 액센트의 soft/line 파생 톤은 시안에서 수작업 값이라
확정 시 명도 규칙으로 재도출 필요. 시안의 서체는 시스템 폰트 폴백이라 프로덕션 인상과
미세하게 다르다.

## 시안 v1 council 판정 반영 — 한돈·AI 노출·시안 v2 (2026-08-16, 커밋 5e3399f·b73a0a7)

**결정과 근거** — 오너 피드백 4건(배포 UI 괴리·한우/돼지 명명 비대칭·LLM 코어·자기보고
과다)을 4인 council(Architect+독립 Skeptic/Pragmatist/Critic)로 심의, 오너가 판정 3건을
승인했다. ① 시안 v1의 문제를 "다름"이 아닌 **정체성 교체**로 재진단 — v2는 배포 체계
(종이+노랑) 유지, 경락가 차트를 잉크 도판으로 재도색해 이식, 서류철 탭·스탬프·카테고리
4색은 보류(로즈·적갈의 판정색 오독 위험 + 심사 전 재QA 비용). ② **돼지→한돈**: 검증
원장(축산물이력제)이 국내산만 조회하므로 "돼지"는 검증 범위 과대 표기 — "라벨=검증 가능
범위" 원칙을 02에 신설하고 라벨 8곳·spec 3문서를 치환(첫 노출 병기 "한돈(국산 돼지)",
매처의 돼지·양돈 별칭 유지, 코드 id·공시 원문 인용부 불변). 파급 실측: 공개 캐시의
"돼지" 2건은 원문 인용부라 제외, 레드팀·필터 하드코딩 0건. ③ LLM은 이미 코어(추출
교차검증·눈높이 서술)라는 재진단 — 자기부정 문구("AI 대화 응답은 준비 중입니다")를 사실
서술로 교체하고 AI 역할 문장을 방법 밴드에 노출, 챗 공개는 06에 신설한 **출시 게이트
4종**(다턴 레드팀·전역 한도 KV·배포 외부 킬스위치·강등 리허설) 통과 뒤로 — 기준은
날짜가 아니라 게이트. ④ 자기보고형 소거를 v2 문안에 적용(주어=자산·데이터·기록,
한계 고지는 유지) — 04 전 표면 승격 개정은 후속.

**트레이드오프** — (a) 검증기까지 통과시킨 카테고리 4색을 보류: 색각 안전성과 판정색
비침범은 별개 문제였다 — 로즈·적갈은 검증기(구분 가능성)를 통과해도 의미(판정 아님)를
보장하지 못한다. 팔레트는 기록으로 보존, 대회 후 재론. (b) "한돈" 인지도가 "돼지"보다
낮다 — 병기·별칭 유지로 완화하되 완전 해소는 아니다. (c) 챗을 게이트 뒤로 두는 대신
당장의 "생성형 AI 활용" 어필은 문안 노출·기획서 서사("AI는 파이프라인 전체에, 단일
장애점에는 없음")가 담당한다. (d) v2에서 대화 모듈을 시안에 포함하되 "게이트 통과 후
공개" 주석을 병기 — 미구현 기능의 시각화라는 위험을 정직 표기로 상쇄.

**eval 영향** — 매처에 한돈 라우팅 테스트 1건, AI 역할 문장 필터 감사 1건 추가 (스위트
707 통과·tsc·lint 클린). 06 출시 게이트 4종이 챗 공개의 기계 검증 가능한 조건 목록이
됐다(강등 리허설 = 실측 항목). 시안 v2는 Playwright 렌더 검증에서 캐스케이드 충돌
1건(.ask button이 칩까지 도색)을 잡아 수정.

**알려진 한계** — 04 "자기보고 전 표면 승격" 조항과 배포 중인 홈·착지 기존 문안의 소거
스윕은 미실시(후속 — 삭제 전 법정 고지 보존 목록 확정이 선행 조건). 한돈 전환은 연정
공유 전이라 v1-draft 개정 상태(미푸시). "준비 중" 3착지의 예고 화면은 시안 패턴만 확정,
구현은 레지스트리 파생 작업과 함께. 챗 게이트의 KV 저장소 선정은 챗 착수 시점으로 유보.

## 레지스트리 파생 셸·칩 온보딩 구현 (2026-08-16)

**결정과 근거** — 카테고리의 UI 단일 진실 `content/categories.ts`를 신설하고 내비·푸터·
홈 그리드·히어로 칩·매처 타입·프로파일 id가 전부 여기서 파생되게 전환했다(카테고리
추가·이탈 = 레지스트리 1건 수정). 온보딩은 여정 문서 §3의 칩 3문답을 홈 밴드
(`OnboardingBand`, `/#start`)로 구현 — 마법사 단계 없이 세 질문을 한 화면에 두고 클릭
즉시 프로파일에 반영(즉시 보상 원칙), 건너뛰기는 상태 없이 성립한다. 답이 생기면 전
페이지 상단에 개인화 상태 표시줄(`ProfileBar`)이 나타나고 초기화가 상시 제공된다(투명성
원칙). 소비자 4종 배선: 확인 질문 8종 걱정 정렬+"내 걱정" 태그(홈·착지 공용), 카테고리
그리드 관심 정렬+한돈 병기, 리포트 눈높이 기본값(profile.level, 수동 토글이 우선).
미연결 착지(한돈·미술품)의 빈 상태는 레지스트리 preview 목록으로 대체 — v2 시안의 예고
패턴 구현이다. 눈높이 문항은 여정 문서의 3지선다를 2지선다("처음 들어요/익숙해요")로
개정 — easy 중복 선택지 2개가 눌림 상태 표시와 1:1로 대응하지 않는 표시 정합성 문제
때문(여정 문서 §3의 사후 개정).

**트레이드오프** — (a) CategoryGrid·ChecklistBand를 클라이언트 컴포넌트로 전환: 정적
서버 렌더의 단순성을 내주고 프로파일 반응을 얻었다. SSR 초기 화면은 기본 순서로
렌더되고 하이드레이션 후 정렬이 적용된다(기본 상태=심사 대상 원칙과 정합 — 프로파일
없으면 변화 없음). (b) 온보딩을 별도 페이지가 아닌 홈 밴드로: 여정 이탈 없는 최저
마찰을 얻는 대신 "나의 확인 목록"의 독립 URL이 없다 — 앵커(`/#start`,`/#checklist`)로
대신한다. (c) 예고 목록에서 시안의 발행사 실명("데이터젠")을 제거하고 "한돈
투자계약증권 3건"으로 중립화 — 익명화 게이트는 시안이 아니라 공개 표면에 걸리는
의무임을 재확인(시안의 실명 표기는 구현 전 교정됨).

**eval 영향** — 신규 테스트 19건: 온보딩 카피 필터 감사 + 선택지-프로파일 어휘 전단사
계약(걱정 4값·눈높이 2값을 정확히 한 번씩 덮는지, short 라벨이 코드로 새지 않는지) +
레지스트리 라벨·병기·예고 전 문안 필터 감사(home-copy 확장). 스위트 742 통과·2 스킵,
tsc·lint 클린, build 완주(21페이지). Playwright 실서버 육안: 온보딩 3선택 → 상태
표시줄·요약·그리드 정렬(한돈 최상단)·체크리스트 정렬(내 걱정 태그)·한돈 예고 목록·
리포트 눈높이 확인. 풀페이지 캡처의 헤더 위치 이상은 sticky 스티칭 아티팩트로 판명
(뷰포트 캡처로 반증).

**알려진 한계** — 온보딩 밴드·상태 표시줄의 상호작용은 유닛 테스트 밖(node 환경 —
순수 함수와 문안 계약만 커버, 화면 검증은 Playwright 육안). 관심 정렬은 홈 그리드에만
적용되고 내비 순서는 고정이다(내비까지 정렬하면 위치 기억을 깨므로 의도적 비적용).
narrative 사전 생성이 easy/pro 2종 모두 캐시에 있어야 눈높이 기본값이 의미를 갖는다
(현 공개 리포트는 충족). 예고 목록 문안은 담당 착수 시 갱신 필요.

## 시각 계층 실장 — 차트·타일·모티프 (2026-08-16, 커밋 1147dad)

**결정과 근거** — 오너 지적("이미지나 아이콘 없이 글만 나열")에 대해 v2 시안의 시각
요소를 실제품으로 이식했다. ① 경락가 잉크 도판 차트: reference 캐시 로더
(`auction-series.ts` — 거세·breedCd 가드·저장값 불신뢰 좁히기)와 클라이언트 차트
(`AuctionMarketSection` — 등급 1++~3 범위 밴드·평균선·결측 월 단선·크로스헤어 툴팁·
원자료 표 뷰)를 한우 착지의 `market` 슬롯으로 실장. 여정 문서의 "RSC 수제 SVG" 계획을
개정 — 호버 상호작용을 위해 데이터는 서버 로드, 렌더·상호작용은 클라이언트로 분리.
② 판정 현황을 한 줄 요약 박스에서 큰 숫자 타일 3개로 개편 — 건수 나열 원칙(비율·게이지
금지)은 그대로, 판정색은 마크에만·숫자는 잉크(색이 아니라 글자가 정보를 짊어짐).
③ 라인아트 아이콘 11종(`site/icons.tsx` — 카테고리 모티프 4: 귀표·축사·액자·건물,
층 3, 입문 카드 4)을 홈 그리드·방법 밴드·입문 카드·착지 히어로에 배선 — 사진·일러스트
대신 기록물 미학과 맞는 스트로크 픽토그램 선택. 차트 전용 토큰 2종(--ds-chart-band·
edge)을 tokens.css에 추가(단일 진실 유지).

**트레이드오프** — (a) 차트가 클라이언트 컴포넌트가 되며 번들이 소폭 증가 — 정적
SVG(번들 0) 대신 탐색 가능성(호버 실측값)을 택했고, 표 뷰가 비마우스 접근을 보장한다.
(b) 사진·실물 이미지 불채택: 라이선스·익명화 리스크 없이 시각 밀도를 얻는 대신 실물감은
포기 — 기록물 세계관에는 픽토그램이 정합. (c) 모티프는 잉크 단색 — 카테고리 4색 보류
결정을 준수하므로 색 식별성은 없다(텍스트 라벨 상시 동반으로 충분).

**eval 영향** — auction-series 계약 테스트 6건(축종 가드·월 형식·등급 경계 없으면 합성
금지·정렬·결측 보존), market-context 문안 9종 필터 감사 추가. 스위트 756 통과, 빌드
21페이지, 실서버 육안(차트·타일·그리드 모티프 요소 캡처 — 풀페이지 sticky 스티칭
아티팩트는 요소 캡처로 반증). 프로덕션 배포 후 /cattle 차트·홈 아이콘 서빙 실측.

**알려진 한계** — 차트는 한우 전용(카테고리 시그니처 모듈의 첫 사례) — 타 카테고리는
담당 데이터 연결 후 같은 패턴으로. 호버는 마우스 전용(터치는 표 뷰가 대체, 포인터
이벤트 일반화는 후속). 9호 사건 타임라인(v2 시안 요소)은 미이식 — 다음 회차 후보.
아이콘은 시각 회귀 테스트 없음(육안 검증만).

## 스캔형 개편 — 카드 그리드·타임라인·절제 (2026-08-16, 커밋 947e9ee)

**결정과 근거** — 오너 피드백 3건 반영. ① 개인화 상태 표시줄 삭제: 전 페이지 상단
상시 노출은 얻는 것(투명성)보다 잃는 것(주의 분산)이 컸다 — 초기화·요약은 온보딩
밴드에 남아 투명성 원칙은 유지된다. ② 아이콘 절제: frontend-design-direction의 "모든
카드에 같은 장식을 붙이지 말 것"에 따라 입문 카드 4종·방법 카드 3종 아이콘을 제거하고
**카테고리 모티프 4종만 유지**(식별 기능이 있는 곳만). ③ 공모별 확인 현황을 세로 행
9줄에서 **카드 그리드**로: 카드당 공모명·청약 배지(진행 중만 노랑)·판정 3값(라벨-숫자
우측 정렬 tabular)·정정 접수·청약 일정 — 정정 접수 현황 행 목록을 카드에 흡수해 세로
목록 2개가 그리드 1개+타임라인 1개가 됐다. 카카오뱅크 카드 리스트의 스캔성을 참조하되
시각 어휘는 기존 토큰 관례(line 테두리·radius-lg·hover 상승) 그대로. ④ 사건 타임라인
이식(v2 마지막 요소): 정정이 있는 공모 중 최신 정정 기준 대표 1건을 골라
제출(baseRcpNo에서 접수일 파생)→정정 접수(amendments의 reportName 원문 표기)→재대조
완료(리포트 rcpNo 일치 시 generatedAt+판정 요약, 불일치 시 "재대조 대기" 정직
표기)로 조립 — event-timeline 슬롯의 계약 의도("사건·정정의 상품 귀속 타임라인") 실현.

**트레이드오프** — (a) 표시줄 삭제로 착지·리포트 페이지에서는 개인화 적용 사실이
보이지 않는다 — 정렬 변화가 조용히 일어난다. 홈 온보딩 밴드가 유일한 상태 창구.
(b) 정정 행 목록을 카드 메타로 압축하며 공모별 정정 이력의 목록형 조망은 사라졌다 —
대표 타임라인+리포트 링크가 대신한다. (c) 타임라인 대표 1건 선정은 "최신 정정" 기계
규칙 — 편집 판단이 아니라 정렬 규칙이므로 중립성 유지, 대신 심사자가 다른 공모의
사건은 카드→리포트로 들어가야 본다.

**eval 영향** — 타임라인 문안 7종 필터 감사 추가(스위트 761). 재대조 대기 분기는
watch 최신본과 리포트 rcpNo 불일치 시 자동 노출 — 감시-재대조 시차의 정직 표기가
화면 규칙이 됐다. 빌드 21p·실서버 육안(카드 그리드·타임라인 요소 캡처)·프로덕션 서빙
실측(profileBar 0건 확인).

**알려진 한계** — 미사용 구 CSS(.offerRow·.offerName·.offerCounts·.offerMeta·.tally)
잔존 — 클린업 백로그. 타임라인은 카테고리당 대표 1건만 — 공모 상세 타임라인은 리포트
정정 이력 챕터가 담당. 카드 그리드는 9건 기준 설계 — 수십 건 규모가 되면 페이지네이션
필요.

## 확인 질문→리포트 다리·판정어 쉬운 캡션 (2026-08-16, 레퍼런스 회차 A1+B1)

**결정과 근거** — 레퍼런스 수집(`docs/planning/프론트엔드-레퍼런스-2026-08-16.md`) 결과를
반영한 첫 회차. ① **A1 다리**: 확인 질문 8가지 중 리포트에 실측 챕터가 있는 6개 항목에
`reportChapter`(헤딩 앵커 id+챕터 라벨)를 부착하고, 홈 체크리스트·카테고리 착지 확인
질문에서 "… 리포트 '실재 확인'에서 실측 보기 →" 딥링크로 연결했다. 입문자-여정.md 4단계
갭("가장 값싼 고효율 갭")이자 AI 인용 UI의 원문 딥링크 관례. 대상 공모는 **기계 선정** —
홈은 전 공모 중, 착지는 해당 카테고리 공모 중 공시 청약 개시일이 가장 최근인 것
(`latestOfferEntry`, 사건 타임라인의 최신-정정 기계 선정 선례와 동일 계열)이며 규칙을
화면에 고지한다("선별·추천이 아닙니다"). 리포트 챕터가 없는 2개 항목(보호 범위·매각
경로 = 입문 안내 담당)은 다리 없음으로 정직 표기 — 테스트로 고정. ② **B1 캡션**: 판정
3값의 쉬운 병기 캡션을 `content/verdict-captions.ts` 단일 진실로 신설("대조 불가"는
"틀렸다는 뜻이 아니라, 대조할 공적 원장 자료가 없었다는 뜻입니다"), 리포트 히어로는
easy 눈높이에서만 tally 아래 병기, 카테고리 착지 판정 타일에는 상시 병기(안내 밀도).
근거: 토스증권의 숫자별 설명조 캡션 관례 + SNU팩트체크 "판단유보" 1급 시민 취급.

**트레이드오프** — 다리 대상 공모를 1건으로 고정하면 다른 공모의 같은 챕터는 한 번 더
이동해야 하지만, 항목마다 공모 선택 UI를 붙이면 체크리스트가 목록 UI로 비대해진다 —
공모명을 링크 문안에 그대로 병기해 어디로 가는지 투명하게 하는 쪽을 택했다. 캡션은
리포트에서 pro 눈높이일 때 숨긴다(전문가에게는 소음) — 착지 타일은 눈높이 토글이 없어
상시 노출로 갈랐다. VerdictHero의 tone→판정 매핑은 뷰모델 tallies가 판정 3값과 1:1이라는
현 구조에 의존한다(뷰모델에 verdict 키를 넣는 확장은 다음 회차로 미룸).

**eval 영향** — home-copy 필터 감사에 신규 문안 전건(캡션 3·다리 라벨 6·규칙 고지) 편입,
다리 매핑 계약 테스트(브리지 6/무브리지 2 고정·앵커 id 형식·라벨 비어있지 않음),
latestOfferEntry 유닛 4건(최신 선정·빈 목록 null·입력 불변·실레지스트리 정합). 스위트
781 통과·빌드 21페이지·실서버 Playwright 검증(홈 다리 6링크·착지 3링크·앵커 스크롤 실측·
pro 모드 캡션 0건·무공모 착지 다리 0건) + 스크린샷 육안.

**알려진 한계** — 다리는 앵커 이동까지만 — 챕터 도착 후 해당 확인 질문과의 상호 하이라이트
(형광펜 의미 고정, 레퍼런스 D1)는 기존 .mark 용례 감사 선행 후. 트랙레코드 챕터는
ReportChapterNav 목차에 없어 다리로만 진입된다(목차 편입 여부는 문서 밀도 판단 대기).
캡션 어휘는 판정 3값 전용 — 근거 상태 5값(EVIDENCE_STATUS_LABELS)의 쉬운 병기는 미착수.

## 정정 감시 홈 표면화·후속 질문 칩·관심 배지 (2026-08-16, 레퍼런스 회차 A2+B2+B3)

**결정과 근거** — 레퍼런스 회차 2번째 묶음. ① **A2 감시 밴드**: 입문자 여정 5단계
갭("재방문 시 정정 유무를 첫 화면에서 인지")을 홈 `WatchBand`로 구현. 서버가 전 공모의
감시 캐시(`loadLatestWatchState`)를 요약 문자열로 빌드해 내려주고, 클라이언트가
localStorage 관심 목록(`useWatchedIds` 신설 — watchlist 스토어의 전체 조회 확장)으로
필터한다 — "화면은 캐시만 읽는다"와 로컬 개인화 원칙을 동시에 지키는 분업. 관심 0건이면
밴드 자체를 숨긴다(기본 상태 완결 원칙, 표시줄 삭제 선례의 '비어 있으면 없음' 계열).
행마다 정정 접수 건수·최근 감시 시각·`#report-watch-heading` 딥링크(A1 다리 재사용).
정정 문안은 `content/watch-band.ts` 단일 진실로 신설하고 CategoryLanding의 중복 하드코딩
문장을 이 모듈로 통일. 근거: 카사의 공시 상설 피드(업계 유일)·뱅카우 투자 후 소식 피드.
② **B2 후속 질문 칩**: 안내 패널 아래 "이어서 볼 만한 질문" 칩 3개 — 목적지별 커리큘럼
맵(`followUpQuestions`)은 기존 예시 질문 레지스트리의 원소만 재사용(신규 질문 없음,
필터 감사 기존 커버). 같은 목적지로 되돌아가는 칩 금지를 테스트 불변식으로 고정. 근거:
Perplexity 실측(답변 후 후속 칩이 초기 칩보다 참여 2배) + IDI '칩=판단 기준 커리큘럼'.
리포트 하단에는 역다리("확인 질문 8가지 보기") 1건 추가 — 스캐폴딩↔실측 양방향 완성.
③ **B3 관심 배지**: 카테고리 그리드에 "내 관심" 태그 + "내 관심 카테고리가 앞에
있습니다 — 네 카테고리는 그대로입니다" 정렬 이유 고지. 체크리스트의 CONCERN_TAG·
CHECK_ORDER_NOTE 관례를 그대로 일반화(신규 CSS 없음). 근거: explainable personalization
(숨김 없는 정렬의 이유 노출이 신뢰·성과 모두 우수).

**트레이드오프** — 감시 밴드 데이터는 빌드 시점 캐시라 배포 사이에는 갱신되지 않는다 —
전 화면이 같은 성질이므로 일관적이고, 행마다 감시 시각을 그대로 병기해 현재성을 정직
표기. 후속 칩은 정적 커리큘럼 맵(맥락 무관 고정) — 다턴 문맥 반영은 챗 라우트(06 게이트
4종) 영역으로 남긴다. 홈 페이지가 async 서버 컴포넌트가 되며 전 공모 감시 파일을 읽지만
공모 10건 규모라 빌드 비용 무시 가능.

**eval 영향** — 신규 문안 전건 필터 감사 편입(watch-band 7종 + FOLLOW_UP_LABEL +
INTEREST_TAG·CATEGORY_ORDER_NOTE). 후속 칩 불변식 3종(전 키 비어있지 않음·레지스트리
원소만·자기 목적지 회귀 금지). 감시 문안 정직성 3종(상태 3값 구분·건수 병기·로컬 저장
고지). 스위트 814 통과·빌드 21페이지·실서버 Playwright(관심 0건 밴드 숨김·2건 표시·
딥링크 스크롤·후속 칩 표시와 클릭 순환·관심 태그 2건·역다리 1건) + 스크린샷 육안.

**알려진 한계** — 감시 밴드는 관심 등록 진입점이 리포트 화면뿐이라 신규 사용자가 밴드를
보려면 리포트를 먼저 방문해야 한다(공모 카드에 관심 토글 추가는 후보). detectionFailed
행 표기는 구현됐지만 현 데이터에 실패 기록이 없어 실화면 검증은 미실시. 후속 칩 노출
이후의 행동 측정(클릭률)은 수집하지 않는다 — 비수집 원칙 유지.

## 라이프사이클 스트립·신고서 구조 정보 (2026-08-16, 레퍼런스 회차 C1+C2)

**결정과 근거** — 레퍼런스 회차 3번째 묶음. ① **C1 라이프사이클**: 업계 표준 상태 머신
(공모→배정·납입→보유→매각·정산, 카사가 가장 명시적)을 두 층으로 실장. 첫째,
`SubscriptionPhase`에 "upcoming"을 신설해 청약 개시 전 공모가 "진행 중" 스타일로
보이던 과대 표기를 교정(배지 문안은 유지 — "청약 D-n"은 이미 예정으로 읽힘). 둘째,
리포트 상단 `LifecycleStrip` — 4단계 고정 수평 스트립, 현재 단계만 노랑 밑줄, 보유 단계
라벨은 자산 종류별(사육·보유/운영·보유). 정직성 규칙: 청약 단계만 신고서 일정로 판정하고
이후 단계는 "공시 접수 대기", 부동산 A(매각 사후 대조 리포트)만 "매각 공시 대조 완료"로
전 단계 완료 표기. 각주로 상시 유통시장 부재(주식 멘탈모델 교정 — IDI ★)를 병기.
② **C2 신고서 구조 정보**: 업계 최대 공백(신고서를 "DART 가서 보세요"로 방치)을 리포트
새 챕터로 채움. `data/offers/filing-facts/{offerId}.json`(스키마 v1, zod 로더) — 공모개요
선례(real-estate-a.json)를 따르는 큐레이션 데이터 계층. livestock-9 실데이터 6항목:
발행인 선배정 10%(1,817주)·청약 미달 시 발행인 인수·예상 사업기간 20~26개월·투자자 부담
비용(청약 200원/건 등)·투자자보호기금 2단 구조·공모가격 산정 근거 — 전부 정정신고서
(20260814003572) 원문에서 추출, 발행사명·농장명 미포함으로 익명화. 확인 질문 다리 확장:
보호 범위·매각 경로 항목이 신설 "신고서 정보" 챕터로 연결되며 8문항 전부 다리 보유.
챕터 내비는 facts 존재 시에만 "신고서 정보"를 노출(빈 앵커 방지).

**트레이드오프** — facts는 파서 자동화가 아니라 큐레이션(수동 추출+구조 검증) — 신고서
서식이 발행사마다 달라 지금 파서를 만들면 유지비가 더 크다고 판단, 대신 스키마·로더·
게이트를 먼저 깔았다. 원문 문언 인용 대신 요약 값을 쓴 것은 익명화 의무(인용문에 발행사명
포함) 때문 — 섹션 참조와 rcpNo를 병기해 원문 검증 경로는 유지. 스트립의 "매각 공시 대조
완료"는 assetKind==real-estate 하드코딩 — 매각 전 부동산 공모가 생기면 리포트 데이터
기반 판정으로 교체 필요. /offers 목록의 진행 섹션은 upcoming 포함으로 바꾸고 제목을
"청약 예정·진행 중"으로 정정(upcoming 신설로 카드가 어느 섹션에도 안 들어가는 회귀를
사전 차단).

**eval 영향** — filing-facts 테스트 3군: 스키마·로더(중복 id 금지·부재 시 null), **익명화
게이트 구조 검사**(9자리 숫자열은 rcpNo뿐·법인 표기·농장명·도로명·번지 패턴 0건 —
public-report 게이트와 동일 문법), **다리 정합 불변식**(체크리스트가 신고서 챕터로 다리를
놓으면 최신 공모에 facts 파일이 반드시 존재 — 신규 공모 추가 시 파일 누락을 테스트가
잡는다). 라이프사이클 파생 6케이스 + 신규 카피 필터 감사. 청약 3상 테스트 교정(upcoming).
스위트 846 통과·빌드 21p·실서버 검증(9호 예정 스트립·1호 접수 대기·부동산 완료 표기·
내비 조건 노출·보호 범위→신고서 챕터 다리 스크롤 실측). 사고 1건: 신규 `.stageRow`가
같은 모듈의 기존 정정 리플레이 클래스와 이름 충돌해 grid로 병합 — lc* 접두로 개명 후
계측 재확인. CSS 모듈 내 접두 규칙의 근거 사례로 기록.

**알려진 한계** — filing-facts는 livestock-9 1건 — 타 공모는 원문 미보유(로컬 raw 없음)로
미수록, 챕터·다리는 최신 공모 기준이라 심사 경로는 성립. 스트립은 청약 일정 기준의 위치
표시일 뿐 배정·납입 실측이 아니다(발행실적 보고 대조가 붙으면 단계 판정을 데이터로 교체).
검증 리포트 리스킨 대상인 OfferCard의 upcoming 스타일은 중립 배지 재사용 — 예정 전용
시각 어휘는 미도입.

## 한우 착지 드릴다운 개편 — 청약 분리·타임라인 귀속·입문 어휘 (2026-08-16, 오너 피드백 3건)

**결정과 근거** — 오너 드릴다운 피드백: ① 청약 중/종료 미분리 ② "왜 9호만 정정 기록이
따로 밑에 있나" ③ 판정 현황·층별 지원 선언이 비친숙. 대응: ① 공모 섹션을 착지 최상단으로
올리고 "청약 예정·진행 중 / 청약 종료 · 사후 검증" 2그룹 분리 — 그룹 어휘는 /offers
목록과 동일(테스트로 고정), 그룹 내 정렬은 접수일순 유지(선별·추천 아님 고지 유지).
② 대표 공모 타임라인의 실패 원인 진단: 기계 선정(최신 정정) 규칙이 화면에 없어 자의적
스포트라이트로 읽혔다. IDI 원칙("사건 공시는 상품에 귀속")대로 **타임라인을 진행 중
그룹의 해당 공모 카드 아래로 귀속** — 정정 있는 진행 중 공모만 전개(9호 = 정정으로 청약
일정이 바뀐 공모라 지금 가장 필요한 정보), 종료 공모는 카드 정정 라인+리포트 챕터가 담당.
카테고리 레벨 스탠드얼론 섹션은 제거. ③ "판정 현황"→"지금까지의 대조 결과" + 맥락 리드
("공모 9건에서 공시된 기재 2,090건을 … 하나씩 대조한 누적 결과입니다" — 실건수 병기),
"층별 지원 선언"→"무엇을 어디까지 대조하나" + 층마다 쉬운 확인 질문("실물이 정말
있는가") 병기, 엔진 층 이름은 mono 부라벨로 강등. 섹션 순서 재편: 공모(상품 우선 — 업계
관례) → 대조 결과 → 시장 차트 → 대조 방법 → 확인 질문 → 특화. 신규 문안은
content/category-landing.ts 단일 진실.

**트레이드오프** — 진행 그룹이 비면 "지금 청약 예정이거나 진행 중인 공모가 없습니다"
정직 표기(그룹 자체를 숨기면 분류 존재를 학습할 기회가 사라짐). 타임라인 귀속 규칙상
종료 공모의 정정 계보는 착지에서 한 단계 멀어졌다(카드 라인→리포트) — 진행 중 공모의
판단 적시성이 종료 공모의 회고보다 우선한다고 판단. 큰 숫자 타일은 유지(v2 시안 시각
계층 승인 이력) — 제거 대신 맥락 문장·위치로 친숙성을 해결.

**eval 영향** — 신규 문안 전건 필터 감사 + 계약 3건(층 쉬운 질문 3종·질문 형식, 대조 결과
리드 건수 병기, 그룹 어휘 = /offers 동일). 스위트 861 통과·빌드 21p·실서버 검증(섹션
순서·2그룹·9호 타임라인 귀속·층 부라벨·preview 카테고리 무영향) + 전체 스크린샷 육안.

**알려진 한계** — 진행 그룹 카드 1건일 때 우측 여백이 큼(그리드 최소폭 유지 — 진행 공모가
늘면 자연 해소). 층 쉬운 질문은 공통 3층 고정 — 카테고리 특화 층이 생기면 질문 사전 확장
필요. 대조 결과 리드의 건수는 판정 3값 합계 기준(가격 위치 제시 건수는 미포함 — 집계 혼동
방지).

## 홈 드릴다운 개편 — 온보딩 다이얼로그·로드맵·아이콘·표기 정리 (2026-08-16, 오너 피드백 5건)

**결정과 근거** — 오너 홈 드릴다운 피드백 반영. ① **온보딩 위치**: 밴드("나의 확인 목록
만들기")를 홈 흐름에서 제거하고 **첫 방문 다이얼로그**로 전환(오너 제안 "처음 팝업").
노출 조건 = 홈 첫 방문 + 프로파일 비어 있음 + 미열람(localStorage jeomjeom.onboarding.v1)
— 리포트 딥링크로 진입하는 심사위원은 팝업을 만나지 않는다. ESC·백드롭·버튼 닫기 전부
열람 처리, 닫기 라벨은 선택 유무로 분기(건너뛰고 둘러보기/이대로 시작하기). 재진입은
푸터 "나의 확인 목록 설정" 버튼(커스텀 이벤트) — 선언형 개인화의 재설정 경로 보존.
② **초기화 버튼·"적용됨: 눈높이…" 요약 제거**(오너 지시) — 칩 자체가 토글이라 해제
경로는 유지된다. ③ **조각투자 첫걸음 = 로드맵**: 카드 그리드를 번호(01~05)+연결선의
순서형 리스트로 재구성 — "어디부터 읽어야 할지"를 구조로 답함. 05번 스텝 "어떻게
대조하는지"가 제거된 방법 밴드를 승계: AI 역할 문장(council 결정 보존) + /methodology
링크만 남김. ④ **방법 밴드 제거**(오너 — "/methodology와 중복, 메인은 필요한 것만"):
METHOD_LAYERS·VERDICT_SENTENCE·coverageSentence 콘텐츠와 CSS까지 정리(사장 코드 청소).
⑤ **한돈 "(국산 돼지)" 병기 제거**(오너) — 검증 범위 서술은 카드 노트·스펙 02에 유지되어
라벨=검증 가능 범위 원칙은 보존. ⑥ **카테고리 모티프 교체**: 귀표·축사(간접 은유)를
소·돼지 얼굴 라인아트(직접 지시)로 재작화 — 외부 다운로드 불채택(라이선스, 기존 결정
계승), 액자·건물은 유지.

**트레이드오프** — 첫 방문 팝업은 진입 마찰이 있으나 오너 결정이며, 홈 한정+1회+즉시
닫기 가능으로 완화. 커버리지 문장("2026 공모 N/8건")은 홈에서 사라짐 — /offers 히어로가
동일 정보를 유지. AI 역할 문장의 홈 노출은 로드맵 05로 축소 승계. StrictMode 이중
이펙트에서 ref 가드+타임아웃 취소 결합으로 다이얼로그가 영구 차단되는 버그를 실서버
검증에서 잡아 수정(가드 설정을 타임아웃 발화 시점으로 이동) — 자동 테스트가 아닌
실브라우저 검증이 잡은 사례로 기록.

**eval 영향** — 온보딩 신규 라벨 3종 필터 감사 편입, 제거된 콘텐츠의 테스트 참조 정리.
스위트 854 통과·빌드 21p·실서버 검증(첫 방문 다이얼로그 표시→칩 선택→라벨 분기→ESC
닫힘→재방문 미표시·초기화/요약 0건·방법 밴드 0건·로드맵 5스텝·국산 돼지 병기 0건·푸터
재오픈) + 다이얼로그·로드맵·그리드 스크린샷 육안.

**알려진 한계** — 다이얼로그 포커스는 열림 시 닫기 버튼 이동만 구현(완전한 포커스 트랩
아님 — 백로그). 온보딩 열람 플래그는 프로파일과 별개 키라 프로파일 초기화 수단이 UI에서
사라짐(칩 개별 해제만 가능). 소·돼지 모티프는 24px 소형 렌더에서 변별이 제한적 — 상시
텍스트 라벨 동반 전제(기존 원칙)로 허용.

## 디자인·모션 고도화 P1~P3 — 히어로·리빌·카운트업 (2026-08-16, PRD 기반)

**결정과 근거** — 오너 요청(kbstarreit.com급 애니메이션·배경, 단 모방 아닌 발전).
레퍼런스 실측(헤드리스 캡처): KB는 jQuery+AOS+영상 1개 — 기술이 아니라 연출 적용 범위가
차이. 팔레트(중립+옐로)는 우리와 구조 동일. PRD
`docs/planning/디자인-모션-고도화-PRD-2026-08-16.md` 수립 — 원칙: **"데이터와 기록물이
우리의 사진"**(실사진·영상 불채택: 라이선스·익명화 게이트·기록물 미학), **신규 의존성
0**(motion/react 보유분 확장, GSAP/AOS/Lottie 불채택 사유 명기). 구현 P1~P3: ① 홈 히어로
풀하이트(≈88dvh) + 우측 **대조 일러스트**(공시 문서↔원장 문서, 노랑 형광펜 행, 양방향
화살표, 검증 스탬프 — SVG stroke draw-in 순차 1.5s, CSS 전용) + 콘텐츠 스태거 인 + mono
SCROLL 큐. ② Reveal(기보유: hydration·reduced-motion 가드)을 리포트 한정에서 홈 4밴드 +
착지 슬롯 5종으로 확장, 로드맵 스텝은 li 단위(Reveal에 as="li"·id 프롭 추가). ③ CountUp
신설(SSR=최종값, 뷰포트 진입 1회 0→실값 0.9s, reduced-motion=정적) — 착지 판정 타일 3개.

**트레이드오프** — 히어로 일러스트는 실사진의 대체물로 추상 기록물을 그림(특정 발행사·
문서 미지칭 — 익명화 게이트 무관). draw-in·스태거는 CSS 전용(모션 JS 부담 0, LCP 영향
없음 — 텍스트는 애니메이션이어도 즉시 페인트 대상 아님? heroIn은 both 채움이라 초기
opacity 0 — LCP 요소(제목)가 0.55s 지연 페인트로 계측될 수 있음, 실측 확인 필요 시 P4에서
조정). 카운트업은 최종값 불변 연출(집계·게이지 아님 — 04 비침범, PRD §5). 회전 배지 등
장식 모션은 motion-ui 원칙(장식이면 제거)으로 불채택.

**eval 영향** — 신규 문안 없음(SCROLL은 aria-hidden 장식). 스위트 854 유지·빌드 21p.
실서버 검증: 히어로 draw-in·스태거·SCROLL 큐(1440), 모바일(375) 일러스트 숨김 실측,
카운트업 중간값 1,369→최종 2,074 실측(값 정확성), 로드맵·착지 리빌 진입 확인. CLS: 리빌은
transform/opacity 전용 + once. reduced-motion 전 경로 폴백(기존 가드 + 신규 CSS 미디어).

**알려진 한계** — P4(질감·워터마크·패럴랙스)·P5(마이크로 인터랙션 정리)는 오너 반응 대기.
히어로 LCP 계측은 프로덕션 라이트하우스 미실시(다음 회차 확인 항목). 일러스트는 68rem
미만 숨김 — 모바일 히어로는 텍스트 전용. 경락가 차트의 배경 장식화는 가격 인상 규제 검토
선행으로 계속 보류.

## 히어로 일러스트 철회 — 실물 사진 투명 레이어 (2026-08-16, 오너 피드백)

**결정과 근거** — 오너 판정: 대조 일러스트는 별로("전 버전이 깔끔"), 스크롤 단위 구분은
유지, 대안으로 "실물 사진 + 투명 레이어" 제안. 일러스트·draw-in CSS 전부 제거하고
사진 레이어로 교체. 사진 선정: **카테고리 중립** 원칙(한우·미술품 등 특정 자산 사진은
4카테고리 균등 훼손) → 서류 더미(기록물) 피사체 = 검증 아카이브 정체성과 일치.
Unsplash 무료 라이선스(유료 Unsplash+ 후보 제외 확인), 발행사·인물 미식별. 처리:
multiply 블렌드(종이톤에 녹아듦) + 4방향 그라디언트 마스크(경계 상자 제거) + 채도
55%·미세 블러 — "투명처리 레이어" 요구 그대로. 텍스트 영역(좌 36%)은 마스크로 비움.
출처·라이선스는 `docs/design/assets.md` 신설 대장에 기록(라이선스 신호등 원칙).

**트레이드오프** — 1차 후보(흰 서류철, 최소주의)는 multiply 후 완전 소실 — 대비 있는
서류 더미로 교체하고 감쇠(38%)로 산만함 상쇄. 사진은 68rem 미만 숨김(모바일 텍스트
우선). next/image priority 로드(가림 없는 above-fold 페이드 팝 방지) — 60vw sizes로
최적화 위임. 리빌·카운트업·풀하이트·SCROLL 큐는 유지(오너 긍정 항목).

**eval 영향** — 스위트 854·빌드 21p. 실서버 스크린샷(1440 사진 레이어·375 숨김) 육안.
중간 실패 2회 기록: ① 흰 사진 소실 ② 마스크 미페이드로 상자 모서리 노출 — 4방향
마스크로 해결.

**알려진 한계** — 사진 1장 고정(시즌·다변화 없음). 원본 411KB — next/image 변환 의존,
프로덕션 LCP 실측은 미실시(P4 확인 항목 유지). 착지·리포트에는 사진 미도입(문서 밀도
표면은 기록물 미학 유지) — 확장은 오너 반응 후.
