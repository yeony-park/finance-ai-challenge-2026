# 미술품 이식 브리프 — 세션 배치용 (2026-08-22)

> 발주: 오너 + 코디네이터 세션. 상위 문서: `docs/planning/팀-자산-이식-계획.md` §4.
> 이 브리프의 범위를 벗어나는 판단이 필요하면 **작업을 멈추고 §8로 회신**한다.

## 0. 환경 (작업 시작 전 순서대로)

1. 작업 디렉토리는 **본인 전용 워크트리** `/home/viowlet/Projects/jeomjeom/wt-port-art` (브랜치 `port/art`). 다른 워크트리(`integration/`, `wt-port-pig/` 등)의 파일은 **읽기만** 허용, 수정 금지.
2. `npm ci` 실행. `.env` 없음 — 전 기능 fake 모드 동작이 원칙.
3. 필독 (순서대로): 워크트리의 `CLAUDE.md` → `AGENTS.md` → `docs/spec/02-vocabulary.md` · `04-expression-rules.md` · `05-data-policy.md` → `docs/design/design-system.md` → 이 브리프 전체.
4. 이식 원천 (읽기 전용):
   - `/home/viowlet/Projects/jeomjeom/wt-hyunsuk` — `lib/domain/calculations.ts`(18함수) · `components/art/charts.tsx`(160줄, 순수 CSS 차트 6종) · `tests/art-calculations.test.ts` · `app/globals.css`의 차트 기하 셀렉터(참고용)
   - `/home/viowlet/Projects/jeomjeom/wt-docs-pig-sto-research/data/art-evidence.ts` — **현재 상품 데이터 표준**(정규화본 5상품, 팀 기결정 `07-asset-map.md`)

## 1. 미션

현석의 아트체크 자산 중 **게이트 무관 안전 지점(P1: 계산 엔진+차트+테스트)** 을 이식하고, **정규화본 5상품 기반으로 `/art` 착지 뼈대(P2)** 를 세운다. 338건 과거 이력 원자료는 라이선스 Yellow(서면 확인 전 공개 화면 금지)라 이번 범위에서 **커밋도 화면 노출도 하지 않는다** — 집계 사실 언급 + 원문 링크 격하만.

## 2. 불변 가드레일 (위반 = 작업 무효)

| # | 규칙 |
|---|---|
| G1 | **4등급 판정(해볼 만함/조건부/주의/위험) 완전 배제** — 팀 확정. 어휘·타입·CSS 클래스·서술 문장 어디에도 유입 금지. 원천의 `verdict` 필드·`AiVerdictBadge`·`AiJudgmentPanel`·verdict 산출기(legacy-adapter.ts:114)는 이식 금지 목록 |
| G2 | 판정 어휘 3값만(일치·원장 불일치·대조 불가). 원천 5값 매핑: verified→일치, mismatch→원장 불일치, review·missing·stale→대조 불가. 금지 어휘: 안전·우수·저위험·매수 적합·등급·점수·추천 |
| G3 | **실명 중립화** — §4 치환표. 플랫폼·발행사·운영사 법인명은 `allowedPublicNames` 팀 결정 전이므로 기본값 = 중립 라벨. **작가명(자연인)은 이번 단계 화면에 올리지 않는다**(정책상 조건부 허용이나 원천 데이터가 Yellow에 종속) |
| G4 | 요청 시점 외부 API·LLM 호출 금지. `/api/ai/*` 4종·`lib/art/ai.ts`(원시 OpenAI fetch) 이식 금지 |
| G5 | 제3자 이미지 렌더링 금지 — CloudFront·플랫폼 CDN 이미지 URL은 커밋 파일에서 제거. 외부 링크는 DART 원문 링크만 유지, `weshareart.com`·`artprice.kr` 딥링크 제거 |
| G6 | 산문 주석 금지. `git restore`/`checkout` 금지. CSS 대량 수정은 시각 검증 없이 완료 처리 금지 |
| G7 | 신규 npm 의존성 설치 금지. 원천의 `as unknown as` 캐스팅은 zod 파싱 또는 명시 타입으로 교정 |
| G8 | push · 배포 · PR 금지. 커밋은 로컬 `port/art`에만 |

## 3. 파일 경계

**생성·수정 허용 (이 목록이 전부):**
- `src/lib/art/` (신설 — `calculations.ts` 이식) + `src/lib/art/__tests__/calculations.test.ts` (원천 테스트 vitest 포팅 — node:test → vitest)
- `src/components/art/` (신설 — 차트 컴포넌트 + `art.module.css`. 차트 기하는 원천 유지, 색은 `--ds-chart-band`·`--ds-chart-edge`·`--ds-ink` 계열로만 재도색)
- `src/lib/content/art.ts` (신설 — 정규화본 5상품의 사실·문안, §4 적용) + `src/lib/content/__tests__/art-copy.test.ts` (신설 — `home-copy.test.ts` 패턴 미러)
- `src/app/art/page.tsx` (기존 — CategoryLanding 계약 유지, `custom` 슬롯에만 추가)
- `docs/worklog/이식-art.md` (신설 — 4섹션 규약)

**마지막 단계에만, 최소 diff로:** `src/lib/content/categories.ts`의 art `note`/`preview` 문안 (사실 범위 내 갱신만. **`preview`를 null로 바꾸지 말 것** — null="검증 리포트 공개 중" 의미, 홈 조각 상태와 연동. 미술품은 아직 리포트가 없다)

**접촉 금지:** `src/styles/tokens.css` · `src/lib/spine/` · `src/lib/verify/` 전체 · `src/components/{home,site,report,landing,category,pig}/` · `package.json` · `data/` (이번 단계 데이터 파일 커밋 없음 — 5상품 사실은 content 모듈 상수로). 필요해 보이면 §8.

**이식 금지 목록 (원천에 있어도 가져오지 않는다):** artists 계열 4라우트 · `lib/catalog.ts`(데드코드) · `data/demo/art-investment.json`(4등급 시연용) · `globals.css` 셀렉터 복붙 · compare 워크스페이스(후속 단계 — 이번 범위 외) · products/platforms 화면(후속 단계).

## 4. 실명 중립화 치환표 (커밋 전 적용)

| 원문 | 치환 |
|---|---|
| 투게더아트 · 주식회사 투게더아트 | `발행사` |
| 아트앤가이드 / 아트투게더 / TESSA·테사 | `플랫폼 A` / `플랫폼 B` / `플랫폼 C` |
| 작가명 (김환기·쿠사마 등 전부) | 화면·커밋 데이터에서 제외 (작품 식별은 "5호 상품" 류 중립 표기) |
| 작품명 (작가명 내장) | 제외 또는 `상품 N` 중립 표기 |
| weshareart.com · artprice.kr · CloudFront URL | 제거 (DART 링크만 유지) |
| DAKER · 아트체크 (브랜드) | 제거 |

DART 접수번호 2건(`20260513000002`·`20260512000391`)·금액·수치·일자는 공적 사실 — 유지.

## 5. 스텝 (순서 고정)

- **P1 계산·차트 (게이트 무관 안전 지점)**: `calculations.ts` 이식 + 테스트 vitest 포팅(원천 10함수 케이스 보존, verdict 관련 어서션은 원천에 없음 — 그대로) → 차트 6종을 `src/components/art/`로 (globals.css의 `.bar-chart` 등 기하를 module.css로 추출, `--ds-*` 재도색). 검증: 신설 테스트 통과 + `npx tsc --noEmit`.
- **P2 콘텐츠 모듈**: `art-evidence.ts`(정규화본)를 §4 적용해 `src/lib/content/art.ts`로 — 5상품의 공모 사실·5값→3값 매핑된 근거 상태·한계 주석. `art-copy.test.ts`: 전 문자열 `filterOutput` 통과 + §4 원문 실명 0건 grep 어서션 + 4등급 어휘 0건 어서션.
- **P3 착지 조립**: `app/art/page.tsx` `custom` 슬롯에 5상품 사실 카드 + P1 차트(수치가 정규화본에서 산출 가능한 것만 — 억지 산출 금지, 자료 없으면 "대조 불가"/"기재 없음" 정직 표기) + 과거 이력은 "플랫폼 3곳 공개 이력 338건 확보 — 이용 확인 절차 진행 중, 원문은 각 플랫폼에서 열람" 취지의 **집계 사실+격하 문구**만.
- **P4 전체 검증**: `npm test` 전건 통과 → `npm run build` → `npx next start` + Playwright 스크린샷(1440·390) **직접 열어 육안 확인**.
- **P5 기록·커밋**: `docs/worklog/이식-art.md` 4섹션. 커밋 원저작 표기: `feat(art): … — 원저작 hyonsho@21a2853 (hyunsuk)` (정규화본 유래분은 `yeony-park@f0237ad` 병기). 마지막에 categories.ts 문안 갱신 별도 커밋.

## 6. 완료 기준

전체 테스트 green · build green · 스크린샷 육안 통과 · 금지어 grep 0건(`grep -rn "투게더아트\|아트앤가이드\|아트투게더\|TESSA\|테사\|해볼 만함\|조건부 해볼\|weshareart\|artprice\|cloudfront" src` 결과 없음) · worklog 존재 · push 안 함.

## 7. 하지 않는 것

338건 원자료 커밋·노출 · 비교 워크스페이스 · products/platforms/artists 화면 · API 라우트 · 4등급 관련 일체 · mask.ts·어댑터·spec 문서 수정(제안만 worklog에) · registry preview null 전환 · 홈 접촉.

## 8. 에스컬레이션·보고

경계 판단이 필요하면(특히: 정규화본에 없는 수치를 화면이 원할 때, 중립 라벨이 문장을 어색하게 만들 때, 공유 파일 수정 욕구, 테스트 회귀) — 진행하지 말고 이 브리프를 보낸 코디네이터 세션에 SendMessage로 회신. 완료 시: 커밋 해시 + 테스트/빌드 결과 + 스크린샷 경로 + 알려진 한계 회신, 오너에게도 자기 세션 대화로 요약 보고.
