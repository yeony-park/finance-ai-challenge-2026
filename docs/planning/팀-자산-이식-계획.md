# 팀 자산 이식 계획 (v1-draft)

> 상태: 초안 — 오너 지시(2026-08-22) 기반. 상위 결정 계승: `docs/spec/07-asset-map.md`(자산 행선지 확정분), `05-data-policy.md`(익명화 예외·라이선스 신호등·이미지 정책), `01-category-contract.md`(착지 슬롯 계약).
> 인벤토리 실측 기준: `wt-hyunsuk@21a2853` · `wt-docs-pig-sto-research@f0237ad` · `origin/Su@6421319` (2026-08-22 전수 조사. 팀원 3개 브랜치 모두 8/15 이후 신규 커밋 없음).

## 0. 오너 지시 (2026-08-22)

1. 기준 틀 = 공통 명세의 카테고리 착지 계약(`REQUIRED_PAGE_SLOTS` 5종: layer-declaration · verdict-summary · evidence-table · event-timeline · review-questions). 팀원이 명세 밖으로 더 구현한 것은 해당 카테고리 페이지에 **최대한 그대로** 추가한다 — 착지점은 `custom` 슬롯.
2. 첫 화면 구성 교체(조각 히어로)와 이식 작업은 병렬 트랙으로 진행한다 (§6).
3. 이식은 오너(+Claude)가 수행하되, 카테고리별 이식 명세를 담당 팀원에게 공유한다 (§5).

## 1. 불변 게이트 — "최대한 그대로"의 한계선

| 게이트 | 근거 | 집행 장치 |
|---|---|---|
| 4등급 투자 판정 불채택 | `07` 결정 4 (확정) | 이식 시 verdict 계열 제거 |
| 판정 어휘 3값 (일치 · 원장 불일치 · 대조 불가) | `02-vocabulary.md` | 5값·4값 어휘는 §4 매핑표로 정규화 |
| 공개 표면 익명화 — 실명은 `allowedPublicNames`(실명+공개 근거) 선언분만 | `05` §1 | 익명화 게이트 테스트 (누출 0건) |
| 요청 시점 LLM 호출 금지 — 화면은 사전 생성 캐시만 | `06` + 비용 실측 | 요청 시점 외부 공공 API 호출도 동일하게 캐시 전환 |
| 라이선스 신호등 — Yellow는 서면 확인 전 공개 화면 금지 | `05` §2 | 원문 링크 + 최소 사실 인용으로 격하 |
| 제3자 이미지 렌더링 금지 · iframe 금지 | `05` §3 | 원문 페이지 링크 텍스트로 대체 |

## 2. 부동산 (`/real-estate`) — 착수 1순위

게이트 부담 최저(Su 데모는 실명 0건·LLM 0건·전량 더미 명시)이고, 통합 엔진에 없는 판정 축을 새로 연다.

**Su 브랜치의 실체**: `test_scripts/` 9파일(2,863줄)뿐 — Python 3단 파이프라인 + 단독 HTML 데모. `README.md`가 "테스트 코드를 그대로 운영에 import하지 않는다"를 명시 → **코드가 아니라 스펙·규율을 이식한다** (작성자 조건 존중).

| 이식물 | 형태 | 행선지 |
|---|---|---|
| **건축HUB 표제부** `getBrTitleInfo` 호출·필드 매핑 15종 | TS 어댑터 신설 | `src/lib/verify/adapters/building-register.ts` — `rtms-trade.ts` 형태(Zod + 캐시 + ok/empty/failed) 준수. **부동산 리포트의 "건물 동일성 = 대조 불가"를 "일치"로 승격시킬 유일한 원장** |
| 법정동코드 `StanReginCd` | 1회 수집 → 정적 코드표 | `data/reference/legal-dong/` (런타임 API 불필요) |
| 키 마스킹 3중 장치 (인증필드 물리 제거 · 에러 메시지 마스킹 · quote/unquote 3변형 치환) | 패턴 이식 | 어댑터 공통 유틸 — §4 공통 승격 |
| 원문 보존 계약 (`raw_file`+`raw_sha256`+전후 해시 검증) | 규약 승격 | `data/reference/` 캐시 공통 규약 (§4) |
| 비교 후보 규칙 (취소거래 `excluded_records` 분리 보존) | 설계 채택 | `judge/price.ts` 대조 검토 — 임계값은 "검증 전 샘플"이므로 재검증 |
| UI 패턴 3종 — 5축 요약 카드 / 접이식 "비교 기준 보기" / 데이터·연결·판정 3축 분리 표시 | custom 슬롯 | `/real-estate` 착지 |
| 종료 상품 자백 필드 (`annualized:false`·`irr:false`·`unreconciled_amount_krw` 등) | 필드 채택 | 종료 리포트 공통 (§4) |
| 제외 | — | Python 3종 그대로 이식(작성자 금지) · 더미 상품 7건(실공모 원칙) · ECOS(3값에서 판정 생산 불가 — 맥락 각주 한정) · R-ONE(Su 실측상 잘못된 표 — `STATBL_ID` 재탐색 후 재론) |

## 3. 한돈 (`/pig`) — 재료 밀도 최고, 관문 4개

재료: DART 접수번호 **13건**(회차별 4문서 ×3 + 소액매출공시 1) + corp_code + 제1호 정산 실측(484두 · 2.56억 매출 · 13.1% 실현수익) + 돼지 경락가 어댑터 + 489줄 법적 경계 문서. **원장 대조 축은 `pigNo`/`lotNo` 미보유로 전 항목 "대조 불가" 확정** — 결함이 아니라 리서치 §1·§15가 선언한 구조적 한계이며, "대조 불가를 정직하게 표시한다"의 실증 사례로 쓴다.

| 이식물 | 판정 | 조치 |
|---|---|---|
| `data/pig-disclosure.ts` (접수번호 12 + 회차 실데이터 + 가격 산식) | 수정 | 발행사·농장 실명 → 중립화, `전북 무주군` → 시·도 단위(`mask.ts` 기준). 접수번호·금액·산식은 공적 사실 — 유지 |
| `lib/pig/opendart.ts` (corp_code + 저장본 폴백) | 수정 | `cache:"no-store"` 요청 시점 fetch → 사전 생성 캐시 전환(게이트 4). `submitter` 실명 5곳 마스킹 |
| `lib/pig/pig-market.ts` (돼지 경락가 — 통합 앱 `auction-price.ts`는 한우 전용) | 수정(소) | CSV 하드코딩 검증(63행/87열) 유연화, `AuctionMonthCache` 인터페이스로 재포장. SHA-256+수집시각+한계 기록 설계는 그대로 |
| 경락가 CSV 원본 (공공데이터포털 15148902 공식 다운로드) | 그대로 | `data/reference/` 배치 (Green) |
| `PigDisclosureGallery`(URL 상태 회차 전환) · `PigMarketInfographic`(무의존 SVG 차트) | 그대로 | custom 슬롯 |
| `PigDisclosureDetail` (5섹션) | 수정 | 실명 헤딩 마스킹, 자유 문자열 배지 → 3값 정규화. **제3호 신고서 vs 제2호 발행실적 모집금액 차이(218,800,000 vs 212,800,000)는 `amend/diff` 축으로 이관** — 이식 즉시 손에 쥐는 실측 발견 1건 |
| `PigDiseaseMap` (ASF) | 수정(대) | **iframe 제거**(`05` §3) → 정적 요약+외부 링크. 발생 2건은 판정 어휘 밖 "맥락" 블록으로 격리. "0건이라도 안전을 뜻하지 않는다" 주의문은 그대로 |
| 5상태 근거 어휘 · `asset-page.tsx` 셸 | 제외 | §4 매핑. 셸은 `CategoryLanding` 중복 — 레이아웃 아이디어만 차용. §7.2 **금지 어휘 목록**(안전·우수·저위험·매수 적합)은 스파인 가드레일에 편입 |
| 489줄 리서치 문서 | 그대로 | `docs/research/` 편입 (`07` 기결정) |

익명화 파급 실측: "데이터젠" 26회 중 공개 표면 6회 + 농장명 3종 + 시·군 2종 + corp_code. `mask.ts` `DOCUMENT_PROFILES`에 pig 프로필 등록이 선행 조건.

## 4. 미술품 (`/art`) — 최대 규모, 라이선스 게이트가 관문

hyunsuk 앱 전량 2,549줄(초압축) — **런타임 신규 의존성 0개** (next/react/react-dom뿐). 판정 집계: 그대로 4 · 수정 28 · 제외 13.

**즉시 착수 안전 지점 (게이트 3종 전부 무관)**: `lib/domain/calculations.ts`(18함수 순수 계산) + `components/art/charts.tsx`(무의존 CSS 차트 6종 — `--ds-chart-*` 토큰 이미 준비됨) + `art-calculations.test.ts` vitest 포팅.

**최대 리스크 — verdict가 서술과 융합**: `legacy-adapter.ts:114-118`이 빌드 타임에 실데이터 5건의 4등급을 산출하며 `headline`/`summary`/`keyReasons` 문장이 verdict 분기와 한 덩어리. 기계적 제거 불가 — **5건 서술 재작성 필요**. 연쇄: 홈 4등급 쇼케이스 섹션(이식 대상 아님 — 무관), 비교표 상단 2행 제거(12행 존치, `07` 조건부 채택과 일치), 데모셋 100K(존재 이유가 4등급 시연 — 이식 제외).

**라이선스·실명 지형** (`05` §1·§2, `07` 기결정):

- 338건(아트앤가이드 187 · 아트투게더 145 · TESSA 6) = **Yellow, 서면 확인 전 공개 화면 금지** → 확인 전까지 원문 링크+최소 사실 인용으로 격하 노출. 수집 스크립트·해시 체계는 그대로 가치.
- 법인 발행인명(예: 주식회사 투게더아트)은 DART 공시 공개분 → `allowedPublicNames` 선언으로 표기 가능 (§5 결정 대기 1).
- 유통 플랫폼명(아트앤가이드·아트투게더·TESSA)은 발행인이 아님 → `05` §1 예외 기준 밖, 결정 필요 (§5 결정 대기 1). 미결 시 중립 라벨(플랫폼 A·B·C).
- 작가명(자연인 48+76+6명)은 정책상 **화면 표기 허용**(공개 시장 정보 범위·AI 전송 불가·판정은 상품 단위만)이나, 데이터 원천이 Yellow라 서면 확인에 종속. `/artists` 계열 4라우트는 **Yellow 해소 전 보류**.
- 플랫폼 CDN 이미지 URL(690+140) = 화면 렌더링 금지(기결정) — 비렌더 격리.
- 현재 상품 데이터 표준 = pig-research **정규화본 5상품**(기결정, 오너 M3 확정) — hyunsuk 원본은 원천 보존, 화면은 정규화본.

**API 4종**: `search`(live 분기 삭제 → 순수 로컬 파서. 파서는 챗 열화 폴백 후보 — `07` 기결정) · `ask-product`(사전 생성 QA 캐시 치환) · `analyze-product`/`compare`(호출 UI 없는 데드 엔드포인트 + 게이트 저촉 — 삭제) · `lib/art/ai.ts`(원시 fetch, 스택 불일치 — 삭제).

**비교 워크스페이스**: LLM 0·외부 의존 0, `useSyncExternalStore`+CustomEvent 패턴은 범용 자산. verdict 행 2개 제거 후 12행으로 채택 (`07` 조건 충족). 타 카테고리 확장 후보.

## 5. 팀원 공유·확인 목록 (거버넌스)

이식 커밋에는 원저작을 명시한다: `feat(art): 차트 모듈 이식 — 원저작 hyunsuk@21a2853` 형식. 판정 규칙·마스킹·공개 경로 변경 발생 시 worklog 4섹션 병행 기록.

| 대상 | 확인·결정 요청 |
|---|---|
| 현석 (미술품) | ① **서면 확인 3겹** — 조사 완료(2026-08-22): 판정·요청 문안은 `플랫폼-서면확인-요청-문안.md`. 요지: 아트앤가이드 Yellow 유지(약관 오게시 발견 — 사본 요청 포함) · **아트투게더 Red 강등 권고**(robots `Disallow: /api` 차단 경로 수집 501회 실측 — 소급 승인 확보 전 격리) · TESSA Yellow(수기라 비저촉, 재표시 승낙만. 자동수집 전환 금지) ② 캡처 방식(deliverables md 상류) 본인 확인 ③ 4등급 제거 + 실데이터 5건 서술 재작성 방침 ④ 현재 상품 = 정규화본 표준 재확인 |
| 연정 (한돈) | ① 발행사·농장 실명 중립화 방침 ② `pigNo`/`lotNo` 발행사 서면 제공 가능성 (원장 축 승격의 유일 경로) ③ ASF iframe → 정적 전환 |
| 문수 (부동산) | ① 건축HUB·법정동코드 API 키 발급 상태 ② R-ONE 상업용 표 `STATBL_ID` 재탐색 ③ "운영 이전 조건" 5항을 TS 어댑터 완성 기준으로 채택 |
| 팀 결정 대기 (신규) | `allowedPublicNames` 등재안 — art: 법인 발행인명(근거: DART 공시), 유통 플랫폼명(근거 미확정 — 기본값: 미등재=중립 라벨) / pig: 발행사 법인명(DART 공시 — 단 기존 livestock 관례는 전면 익명, **관례 유지가 기본값**) |

## 6. 실행 순서와 병렬 트랙

```
트랙 A (프론트)                          트랙 B (이식)
─────────────────                        ─────────────────
조각 히어로 시안 v3 → 오너 반응           B1. 부동산: building-register.ts 신설
→ HomeHero 개편 + HeroShards 신설            + 마스킹 패턴·원문 보존 규약 (§4 공통 승격 포함)
→ home.module.css · design-system.md     B2. 한돈: 마스킹 프로필 → 데이터·컴포넌트 이식
                                              → amend/diff 실측 1건 → 착지 채움
                                         B3. 미술품 P1: calculations + charts + 테스트 (안전 지점)
                                         B4. 미술품 P2+: verdict 제거·서술 재작성 → 착지 채움
                                              (Yellow 서면 확인과 독립적으로 진행 가능한 범위까지)
```

- 두 트랙의 파일 집합은 서로 겹치지 않는다 (A: `components/home/*` · B: `adapters/`·`data/`·카테고리 착지). 유일한 접점은 `content/categories.ts`의 카테고리 `note`/`preview` 문안 — 착지가 채워질 때 트랙 B가 갱신하고, 트랙 A의 조각 클러스터는 레지스트리 파생이므로 자동 반영.
- 선행 정리 1건: 미커밋 KRDS 타입 스케일 diff(9파일 + worklog)를 별도 커밋으로 먼저 분리할 것 — 두 트랙 어느 쪽과도 섞이면 분리 불가.

## 7. 공통 승격 자산 (카테고리 무관)

| 자산 | 출처 | 행선지 |
|---|---|---|
| 키 마스킹 3중 패턴 (에러 메시지 포함) | Su | 어댑터 공통 유틸 + 스파인 가드레일 후보 |
| 원문 보존 계약 (`raw_sha256` + 전후 해시 검증) | Su·연정·리서치 §11.3 3자 일치 | `data/reference/` 공통 규약 |
| 금지 어휘 목록 (안전·우수·저위험·매수 적합) | 리서치 §7.2 | `spine/guardrail` 규칙 |
| localStorage 비교함 (`useSyncExternalStore`+CustomEvent) | hyunsuk | 전 카테고리 비교 기능 기반 |
| 종료 상품 자백 필드 세트 (`unreconciled_amount_krw` 등) | Su | 종료 리포트 공통 |
| 5상태→3값 매핑 규칙 (verified→일치 / mismatch→원장 불일치 / review·missing·stale→대조 불가) | 연정 | 이식 시 정규화 기준표 |
