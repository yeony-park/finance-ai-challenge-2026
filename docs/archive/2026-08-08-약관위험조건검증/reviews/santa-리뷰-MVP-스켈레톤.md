# Santa Method 리뷰 로그 — 약관 분석 MVP 워킹 스켈레톤 (2026-08-08)

> 독립 리뷰어 2인 × 동일 루브릭 8기준 × 컨텍스트 격리. 두 리뷰어 모두 PASS여야 NICE.
> 라운드마다 새 리뷰어(기억 없음)로 재검증. 최대 3라운드.

## 결과: 라운드 3에서 NICE (수렴)

| 라운드 | 리뷰어 B | 리뷰어 C | 크리티컬(병합) | 조치 |
|---|---|---|---|---|
| 1 | FAIL | FAIL | 출력 필터 위음성 2건 — "무효다" 활용형 미차단, "승소 보장" 계열 미차단 (양측 수렴) | 룰 보강 + 스펙 문구 기반 테스트 14건 추가 + 제안 5건 반영(불변식 검증 순서, 변수명, 리밋 위치, evidence 필터 어서션) |
| 2 | FAIL | FAIL | 잔존 1건 — 주어("조항/약관") 근접 앵커 때문에 bare형("무효다.")·원거리 주어·"특약" 명사 통과 (양측 수렴, node 실행 재현) | 룰 재설계(주어 앵커 독립 대안화, 판단/확정/확인 활용형, 부정형 lookahead) + 테스트 22건 확장 + 라이브 plainSummary JSON 계약 + 고지 상시 푸터 |
| 3 | **PASS** | **PASS** | 0건 | 종료 |

## 최종 검증 상태 (라운드 3 리뷰어들이 직접 실행)

- `npx tsc --noEmit` clean · `npm test` **79/79** · `npm run redteam` **12/12** (도메인 공격 3종 포함)
- fake 모드 라이브 스모크: 3탭 → 경고 소견+근거 카드 → 고지 문구 완주
- 필터 프로브 34/34: 스펙 명시 단정 표현·패러프레이즈 차단, 완곡·인용형("무효가 될 수 있습니다" 등) 통과 — 위양성 0
- 등급 불변식: 자동 부착 심결례 카드 **이전**에 핵심 근거만으로 검증 → 무력화 불가
- 표준약관 self-scan 편차 0건 (자기 오탐 없음)

## 이월 항목 (비차단 제안 — 다음 이터레이션 백로그)

1. 레이트리미터 hits Map 무한 증가(만료 키 미회수) — 배포 전 키 상한/청소 필요
2. "100% 이깁니다"·"무효로 보아야 합니다" 류 정규식 원리적 한계 패러프레이즈 — 실모델 연결 시 LLM 기반 2차 필터 검토
3. **데모 코퍼스에 '주의' 등급 예시 상품 없음** — 시연 설득력 위해 단일 근거 상품 1건 추가 권장
4. 실데이터 도입 시: 시연 배너 상품 단위 전환, snippet RRN 마스킹, 표준 대비 누락 조항 카운트, 조번호+제목 복합 키
5. live 모드 plainSummary에 코퍼스 컨텍스트 주입(품질), GET 레이트리밋
6. company-defined-basis 마커의 프록시 기준선 재설계 (실데이터 확장 전)

## 메트릭

- First-pass rate: 0% (라운드 1 FAIL — 필터 위음성)
- 수렴 라운드: 3 (max 도달, 에스컬레이션 없이 수렴)
- 리뷰어 합의율: 라운드 1·2 크리티컬 100% 양측 수렴 → 루브릭 유효성 방증
- 이슈 분류: 전 라운드 크리티컬이 모두 "출력 필터 위음성"(컴플라이언스 계열) — 정규식 가드레일의 체계적 약점으로 기록, 실모델 단계에서 LLM 분류기 병행 근거
