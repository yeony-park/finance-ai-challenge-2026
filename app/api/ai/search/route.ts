import { NextResponse } from "next/server";import { aiMode, isParsedSearchQuery, parseSearchLive } from "@/lib/art/ai";import { parseDemoSearchQuery } from "@/lib/art/search";export async function POST(req:Request){try{const {query}=await req.json();if(typeof query!=="string"||!query.trim())return NextResponse.json({error:"검색어가 필요합니다."},{status:400});let parsed=parseDemoSearchQuery(query);let fallback=false;if(aiMode()==="live"){try{const live=await parseSearchLive(query);if(!isParsedSearchQuery(live))throw new Error();parsed=live}catch{fallback=true}}return NextResponse.json({parsed,mode:aiMode(),fallback})}catch{return NextResponse.json({error:"검색 요청 형식이 올바르지 않습니다."},{status:400})}}
