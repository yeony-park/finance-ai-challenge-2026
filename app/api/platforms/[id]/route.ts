import { NextResponse } from "next/server";import { datasetSummaryRepository, platformRepository } from "@/lib/repositories/art-repositories";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const id=(await params).id,p=platformRepository.getById(id);return p?NextResponse.json({...p,summary:datasetSummaryRepository.getByPlatformId(id),trackRecords:platformRepository.getTrackRecords(id),products:platformRepository.getProducts(id)}):NextResponse.json({error:"not found"},{status:404})}