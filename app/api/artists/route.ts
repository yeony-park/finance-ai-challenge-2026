import { NextResponse } from "next/server";import { artistRepository } from "@/lib/repositories/art-repositories";export async function GET(){return NextResponse.json({items:artistRepository.getList(),mode:"mixed"})}