import { NextResponse } from "next/server";import { artistRepository } from "@/lib/repositories/art-repositories";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const id=(await params).id,p=artistRepository.getById(id);return p?NextResponse.json({...p,auctions:artistRepository.getAuctions(id),products:artistRepository.getProducts(id)}):NextResponse.json({error:"not found"},{status:404})}