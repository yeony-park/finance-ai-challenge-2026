import test from "node:test";import assert from "node:assert/strict";import { isAnalysisResult, isParsedSearchQuery, researchToolInterfaces } from "../lib/art/ai.ts";test("AI structured output validation",()=>{const valid={offeringId:"x",verdict:"caution",headline:"h",summary:"s",keyReasons:[],priceInsight:{},artistInsight:{},exitInsight:{},platformInsight:{},evidenceIds:[]};assert.equal(isAnalysisResult(valid),true);assert.equal(isAnalysisResult({...valid,verdict:"unknown"}),false);assert.equal(isParsedSearchQuery({offeringStatus:["open"]}),true);assert.equal(isParsedSearchQuery("x"),false)});test("research tool contracts are present",()=>{for(const name of ["searchWebSources","getProductFacts","getArtworkFacts","getArtistAuctionRecords","getComparableWorks","getPlatformTrackRecord","calculatePriceMetrics","calculateArtistMetrics","calculateExitMetrics","saveEvidence","saveAnalysis"])assert.ok(researchToolInterfaces.some((item)=>item===name))})