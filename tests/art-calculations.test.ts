import test from "node:test";import type { AuctionRecord, TrackRecord } from "../lib/art/types.ts";import assert from "node:assert/strict";import { averageDelayMonths, averageExcludingHighest, auctionVolumeSince, medianAuctionPrice, onTimeLiquidationRate, priceDifference, pricePremiumRate, sellThroughRate, unexplainedDifference, unsoldRate } from "../lib/domain/calculations.ts";
const costs=[{category:"fee",label:"비용",amount:9_000_000}];test("공모가 계산",()=>{assert.equal(priceDifference(130_000_000,120_000_000),10_000_000);assert.equal(Number(pricePremiumRate(130_000_000,120_000_000)?.toFixed(1)),8.3);assert.equal(unexplainedDifference(130_000_000,120_000_000,costs),1_000_000);assert.equal(pricePremiumRate(10,null),null)});const auctions=[{result:"sold",normalizedPriceKRW:100,auctionDate:"2025-01-01"},{result:"sold",normalizedPriceKRW:300,auctionDate:"2024-01-01"},{result:"unsold",normalizedPriceKRW:null,auctionDate:"2023-01-01"},{result:"sold",normalizedPriceKRW:500,auctionDate:"2022-01-01"}] as AuctionRecord[];test("경매 계산",()=>{assert.equal(Number(sellThroughRate(auctions)?.toFixed(1)),75);assert.equal(Number(unsoldRate(auctions)?.toFixed(1)),25);assert.equal(medianAuctionPrice(auctions),300);assert.equal(averageExcludingHighest(auctions),200);assert.equal(auctionVolumeSince(auctions,"2026-01-01",3),3)});const tracks=[{status:"liquidated",delayDays:0},{status:"delayed",delayDays:120},{status:"operating",delayDays:null}] as TrackRecord[];test("플랫폼 계산",()=>{assert.equal(Number(averageDelayMonths(tracks)?.toFixed(1)),3.9);assert.equal(onTimeLiquidationRate(tracks),50)})